version: "2"

linters:
  settings:
    errcheck:
      # Exclude Close() calls — errors are not actionable after a read.
      # io.Closer.Close was excluded by golangci-lint v1 defaults.
      exclude-functions:
        - (io.Closer).Close
        - (io.ReadCloser).Close
